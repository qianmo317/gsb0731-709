# WordVault 离线兜底提示「Translation unavailable」产生与滞留的链路分析

## 一、结论先行

1. **那句英文提示不是三级翻译策略（Google API → Google Web → MyMemory）中任何一级返回的内容**，而是 `background.js` 的 `handleLookup` 函数内部、在调用策略链之前预设的一个**本地默认值**（第四级、也是真正意义上的最后一级兜底）。三级策略全部抛出异常后，异常被捕获并静默吞掉，这个默认值就被当作"翻译结果"继续往下走。
2. **它消不掉的原因是"缓存污染"**：含有这句兜底文案的记录在断网当场就被无条件写进了本地 IndexedDB 生词本，且写入时没有任何标记区分"真翻译"和"兜底文案"。之后每次划同一个词，查询都走"数据库命中"分支，该分支**只负责把复习次数加一，永远不会重新请求网络翻译**。于是这句提示成了永久缓存，复习次数却照常累加。
3. 翻译面板从头到尾**不知道翻译失败过**：后台返回的响应里成功标志为真、不携带任何"翻译降级"信息，面板只能把这句英文当成正常译文渲染。

---

## 二、从划词到面板渲染的完整链路

### 第 1 步：快捷键触发（background.js）

用户在网页中选中文本后按下 Alt+Q。`manifest.json` 里注册了名为 translate-word 的命令，`background.js` 顶部的命令监听器收到后，查出当前活动标签页，向其中的内容脚本投递一条 TRIGGER_TRANSLATION 消息。若内容脚本尚未注入（比如插件刚重载而页面没刷新），则改为向页面注入一个"请刷新页面"的黑色 Toast——这与本次问题无关，不展开。

### 第 2 步：选区校验与 Loading（content.js）

内容脚本 `content.js` 收到 TRIGGER_TRANSLATION 后执行选区处理函数：取出选中文本并去空白；为空则弹 Toast"获取选区文本失败"；超过 800 字符则弹 Toast"选区文本过长"。校验通过后，根据选区的屏幕坐标计算面板位置，先在页面里插入一个带 Shadow DOM 的宿主节点，显示"查询中..."的 Loading 气泡。随后通过运行时消息把 LOOKUP_WORD 请求（携带原文）发给后台 Service Worker，并等待响应。

### 第 3 步：后台查询——先查本地库（background.js 的 handleLookup）

后台收到 LOOKUP_WORD 后，把词转小写去空格，**第一步永远是查本地数据库**（Dexie 包装的 IndexedDB，库名 WordVault，表 words 以词为主键）：

- **命中**：直接在记录上把复习次数加一、刷新更新时间、写回数据库，然后把整条记录连同"来源为本地库"的标记返回给面板。**这条路径完全不碰网络**。
- **未命中**：才进入网络获取流程，即下一节的三级策略链。

这一步是整个问题的分水岭：断网时第一次划词走的是未命中路径；而之后无论网络是否恢复，只要这个词已入库，就都走命中路径。

### 第 4 步：三级翻译策略链与真正的兜底（background.js）

未命中时，代码先声明一个局部变量，**初始值就是那句英文提示 "Translation unavailable"**。然后尝试调用策略调度器：

1. 第一级请求 Google 翻译 API 域名（translate.googleapis.com）；
2. 失败后请求 Google 翻译 Web 域名（translate.google.com）；
3. 再失败则请求 MyMemory 免费接口（api.mymemory.translated.net）；
4. 三级全部抛异常时，调度器抛出一个"全部备份失败"的错误。

**关键点**：调度器抛出的这个错误并不会中断流程——外层用 try/catch 把它接住，仅在控制台打一条日志，随后继续执行。由于没有任何一级给那个局部变量赋过值，它就保持着初始的英文兜底文案。也就是说：

- 这句提示的"产地"不在三个网络请求中的任何一个，也不在调度器抛出的错误对象里，而在 `handleLookup` 内部的变量初始化处；
- 断网只是让三级策略全部失败的触发条件，任何导致三接口同时不可用的情况（DNS 污染、接口变更等）都会产生同样结果；
- 紧随其后的词典接口（api.dictionaryapi.dev，提供音标、词性、例句）失败时同样被静默忽略，所以断网划出的词连音标、词性、例句字段都是空的。

### 第 5 步：兜底文案被当作正常数据写入生词本

翻译流程结束后（无论拿到真译文还是兜底文案），代码**无条件**组装一条新记录：词、原始大小写形式、翻译字段（此时即那句英文提示）、空的音标/词性/例句、复习次数置为 1、当前时间戳，然后写入 IndexedDB，并以"成功、来源为 API"的响应返回给内容脚本。

这里有两个设计事实共同导致缓存污染：

- 写入逻辑不检查翻译字段的内容是否为兜底值，也不检查策略链是否真的成功过；
- 记录结构里没有任何字段（如"翻译是否可用"的标志位）能把这条脏数据与正常数据区分开，因此事后也没有任何机制能识别并修复它。

### 第 6 步：面板渲染（content.js 的 renderPopup）

内容脚本收到响应后只判断一件事：响应里的成功标志是否为真。由于第 5 步返回的恒为成功，面板便正常渲染：把翻译字段原样填进卡片的释义区域——于是用户看到的就是那句英文提示，外观上和一个"正常的英文释义"无异，没有任何错误样式或重试入口。卡片其余部分（音标、词性标签、例句区）因为字段为空而自动不显示。底部显示"已复习 1 次"（即新记录的初始计数）。

### 第 7 步：生词本弹窗的读写（popup.js）

点击浏览器工具栏图标打开的插件弹窗，直接连同一个 IndexedDB，按更新时间倒序列出所有词。断网划入的那条记录同样出现在列表里，翻译栏显示的仍是那句英文提示——弹窗侧同样没有任何清洗逻辑。

---

## 三、断网 → 恢复网络后的完整时序复盘

**断网时第一次划词**：本地库未命中 → 三级翻译策略全部失败 → 异常被吞、兜底文案保留 → 词典接口失败被吞 → 以兜底文案组装记录（复习次数 1）写入本地库 → 面板显示英文提示。词"进生词本"就是这一次写入，且写进的是脏数据。

**网络恢复后第二次划同一个词**：本地库**命中**（那条脏记录还在）→ 命中分支把复习次数从 1 加到 2 并写回 → 原样返回缓存记录 → 面板再次显示同一句英文提示。此后每划一次，计数加一，提示依旧。若用户在面板上点击"+1 复习"按钮，还会通过 INCREMENT_COUNT 消息在已经虚高的计数上继续累加。

**为什么系统"自愈"不了**：命中分支没有任何重新校验翻译有效性的逻辑——不看记录内容、不设过期时间、不与网络结果比对。兜底文案一旦入库，在这个数据模型里就是合法的最终状态。

---

## 四、问题根因定位（落到具体代码行为）

1. **兜底层级张冠李戴**：README 宣传的"三级灾备"指三个翻译接口，但真正产出用户可见提示的是第四级——`handleLookup` 里翻译变量的本地默认值。它绕过了所有错误上报，以普通字符串的形态混入正常数据流。
2. **失败被成功化**：策略链全灭时，响应的成功标志依然为真，且不附带任何降级提示，导致内容脚本的"成功才渲染"判断形同虚设——渲染的就是失败状态。
3. **脏数据无标记持久化**：兜底文案与真实译文共用同一字段、同一写入路径，入库后即不可区分。
4. **命中路径只加计数不复核**：本地库命中时无条件自增复习次数并直接返回缓存，从不重新翻译。这是"提示消不掉、复习次数照涨"这一对现象的共同来源——两者其实是同一行逻辑（命中即自增并返回）的两个侧面。

---

## 五、衍生场景：翻译成功但词典接口失败（"半残记录"）

### 场景设定

前两个翻译源（Google API、Google Web）均超时，第三级 MyMemory 成功返回了真实译文；但紧接着请求词典接口（api.dictionaryapi.dev，负责音标、词性、例句）失败。

### 此时面板上显示什么

面板**正常渲染，但只渲染一半内容**：

- **有的部分**：单词标题、MyMemory 返回的真实中文译文、底部"已复习 1 次"和"+1 复习"按钮、笔记输入区（显示"暂无笔记"）。
- **没有的部分**：标题行右侧的音标位置是空白（连方括号都不会出现）；词性标签整个不渲染；"例句"分区整个不渲染。

判断依据在面板的条件渲染逻辑：音标、词性标签、例句区三处都是"字段为空就不输出对应结构"的写法，而词典接口失败时其异常被静默忽略，元数据对象保持为空对象，组装记录时音标和词性兜底为空字符串、例句兜底为空数组——三个字段恰好全部落入"不渲染"分支。译文字段则是真实翻译，所以释义区正常显示。

对用户而言，这个面板看起来只是"这个词没有音标和例句"，**没有任何迹象表明是词典接口挂了**——这与 README 中"网络差时可能没有音标和例句，会降级处理"的描述一致，属于设计上刻意为之的静默降级。

### 这条"半残记录"之后有自动补全的机会吗

**没有，永远不会。** 依据就是第一轮得出的命中分支结论：

1. 这条记录入库时就是音标空、词性空、例句空的形态，且**没有任何字段记录"词典数据缺失是因为接口失败"**——它与"这个词本身就没有音标和例句"的正常记录在数据上完全同构，系统无从区分。
2. 之后每次划同一个词，`handleLookup` 的本地库命中分支只做两件事：复习次数加一、原样返回缓存。该分支**既不重新调翻译接口，也不重新调词典接口**——补全所需的唯一数据来源在命中路径上根本不会被触达。
3. 全代码库中不存在任何针对缺失字段的后台补偿任务、过期刷新机制或重试入口；面板上唯一的交互（+1 复习、保存笔记）只会修改计数和笔记数组，同样碰不到音标和例句字段。

于是这条记录被永久定格在"有译文、无音标、无词性、无例句"的状态，在生词本弹窗里也同样只显示词性和译文两栏中残缺的那一半（词性徽章位置会显示兜底文案"单词"）。

### 与"断网兜底文案"场景的对比

两个场景是同一根因（脏数据无标记持久化 + 命中路径不复核）的两种表现，但严重度不同：

- 断网场景：翻译字段被污染，用户看到的是错误数据，属于**显性错误**；
- 本场景：只有元数据缺失，译文是真的，属于**隐性残缺**——用户甚至不会意识到数据不完整，更不可能想到要去修复它。

---

## 六、衍生场景：同一词的大小写变体（"Apple" 与 "apple"）

### 场景设定

第一次在某篇文章里划了句首大写的 "Apple"（假设词典义），之后在另一篇文章里划了全小写的 "apple"。

### 生词本里最后有几条记录

**只有一条。** 判断依据是记录唯一标识的生成规则：`handleLookup` 收到面板发来的原文后，**先做"转小写 + 去空白"的归一化变换，再拿变换结果去查库**；数据库主键就是这个归一化后的词。两次划词的原文大小写不同，但归一化结果相同，因此：

- 第一次（大写）：未命中，新建记录写入，主键为小写形态，复习次数 1；原始拼写 "Apple" 被单独存进"显示用词"字段。
- 第二次（小写）：归一化后**命中**第一次那条记录，走命中分支——复习次数加一、原样返回缓存，**不会新建记录**。

也就是说，大小写归一化发生在"查库之前"而非"查库之后"，这决定了它不可能产生重复记录。

### 面板上的拼写以哪一次为准

**永远以第一次为准。** 面板标题优先渲染记录里的"显示用词"字段（为空时才退而显示主键小写形态），而该字段**只在新建记录时赋值一次**，命中分支根本不触碰它。所以：

- 第一次划的是 "Apple"，之后无论再划多少次小写 "apple"，面板标题和生词本列表里都显示 "Apple"；
- 反过来，如果第一次划的是小写、后来才划到大写，显示的就是小写形态。

即"先入为主、首写锁定"——显示拼写与翻译、音标、词性、例句一样，属于首轮就冻结的字段。

### 写入后哪些字段还会变（本轮验证后的完整清单）

记录首次写入后，全代码库中只有三个字段会被后续操作修改：

1. **复习次数**：命中分支自动加一，以及面板"+1 复习"按钮通过 INCREMENT_COUNT 加一；
2. **最后更新时间**：命中分支每次刷新（这一点见下方"矛盾与勘误"）；
3. **用户笔记数组**：保存笔记时追加。

其余字段（显示拼写、翻译、音标、词性、系统例句）自首次写入后永久冻结，与第一、二轮的结论一致。

### 与前两轮的矛盾与勘误

实质性矛盾：**没有**。本轮结论（命中即返回缓存、冻结字段不更新）与前两轮完全自洽，大小写归一化只是进一步明确了"唯一标识是什么"。

但前两轮有一处**不精确表述**需要勘误：

- 第二轮在论证"半残记录无补全机会"时，把命中分支描述为"只做两件事：复习次数加一、原样返回缓存"。实际上命中分支做了三件事——**还会把最后更新时间刷新为当前时间并整体写回记录**。这个遗漏不影响第二轮的结论（刷新时间戳同样碰不到音标和例句字段），但它有一个可观察的副作用：生词本弹窗按最后更新时间倒序排列，所以每次划词都会把该词顶到列表最前面，"最近复习"而非"最近新增"的排序语义正来源于此。第一轮正文对命中分支的描述（"自增计数、刷新更新时间、写回"）是准确的，仅第二轮的概括漏掉了时间戳这一项。

---

## 七、附：涉及的存储结构

本地存储为 IndexedDB 数据库 WordVault，内含一张 words 表，以小写词为主键，另对复习次数和更新时间建了索引。单条记录包含：词（主键）、原始显示形式、翻译、音标、词性、系统例句数组、用户笔记数组、复习次数、最后更新时间。笔记通过面板输入框保存时追加进用户笔记数组；插件弹窗按更新时间倒序读取全表展示。
