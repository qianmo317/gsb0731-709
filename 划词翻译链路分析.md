# 划词翻译链路与离线兜底行为分析

本文档合并梳理三轮分析：第一轮是从划词到面板渲染再到生词本读写的完整链路，并解释断网兜底文案为何消不掉；第二轮分析翻译源成功但词典接口失败时的面板表现与后续补全机会；第三轮分析同一单词不同大小写的归并与拼写显示。三轮结论一脉相承，文末附矛盾检查。

---

## 第一部分：完整链路与断网兜底固化

### 1. 问题结论速览

断网时出现的英文兜底提示是 background 脚本中硬编码的字符串 `Translation unavailable`，它在三个翻译接口全部请求失败后作为默认值保留下来。真正导致"网恢复后仍消不掉"的根本原因不是网络层，而是本地数据库（IndexedDB）的"先查缓存、命中即返回"策略：断网那次已经把这条带兜底文案的记录写进了生词本，之后每次划同一个词都在本地命中，代码直接返回缓存并自增复习次数，根本不会再发起任何网络请求，于是兜底文案被永久固化。

### 2. 触发入口：快捷键到 content script

扩展通过 `Alt+Q` 触发。background 中监听到 `translate-word` 命令后，向当前活动标签页发送 `TRIGGER_TRANSLATION` 消息。

content script 收到该消息后调用 `processSelection()`，读取 `window.getSelection()` 的文本，做长度校验（空文本或超过 800 字符弹 toast 拦截），计算选区坐标，先显示"查询中..."浮层，然后通过 `chrome.runtime.sendMessage` 发送 `LOOKUP_WORD` 消息给 background。

content script 本身不做任何翻译或离线判断，只负责把选中的词原样转发，然后等待 background 返回。

### 3. 核心调度：background 的 handleLookup

真正决定行为的是 handleLookup，执行顺序固定为两步。

**第一步：本地查库。** 调用 `db.words.get(word)`，以小写化后的单词为主键查 IndexedDB。

- 如果命中：立即把 `record.count` 加 1，更新 `lastUpdated`，写回数据库，然后返回 `{ success: true, data: record, source: 'DB' }`。这一分支完全不接触网络，不校验翻译内容是否有效，也不判断记录是否在离线状态下生成。
- 如果未命中：才进入第二步网络请求。

**第二步：网络请求。** 这是兜底文案的直接产生点。代码预先声明变量并赋默认值 `let trans = "Translation unavailable"`，随后调用 `fetchTranslationStrategy(rawWord)`。策略分发器依次尝试三个翻译源：Google Standard、Google Web、MyMemory。每个 provider 内部都是裸 fetch，没有超时控制。断网时 fetch 立即 reject，异常被分发器中各自的 try/catch 捕获并打印日志，三个都失败后分发器抛出错误。

这个抛出的错误被 handleLookup 中包裹翻译请求的 catch 捕获，仅打印日志，不重新抛出，也不把 success 置为 false。于是 trans 保持为兜底值，程序继续往下走。

紧接着请求词典接口拿音标、词性、例句，这个请求被独立的 try/catch 包裹，失败时元数据保持为空对象，不影响主流程。

### 4. 兜底文案被写入生词本

随后组装新记录，translation 字段就是兜底值，音标、词性为空，例句为空数组，count 硬编码为 1。然后无条件 `db.words.put(newRecord)` 写入 IndexedDB，并返回 `success: true`。

这就是"断网划词却进了生词本"的直接原因：代码把"三个接口都失败"当成"成功但内容不可用"的状态，而不是失败状态。没有任何逻辑判断 trans 是否等于兜底字符串、网络是否可达，也没有在接口全失败时阻止落库。

数据库使用 Dexie 封装 IndexedDB，words 表以 word 为主键，同一个词再次 put 会覆盖，但由于命中分支根本不走新记录 put，而是走更新 count 的分支，所以兜底文案一旦写入就不会被覆盖。

### 5. 面板渲染：前端只认 success 标志

content script 收到响应后，只判断 `res.success`。由于离线兜底时返回的是 success: true，前端直接调用 renderPopup，把 data.translation 原样插入翻译区，于是用户看到那句英文。渲染层没有对兜底文案做任何检测、标红或重试提示，它把兜底字符串当成正常翻译结果展示。

### 6. 网恢复后为什么消不掉

网络恢复后再次划同一个词：handleLookup 开头本地查库命中断网时写入的记录，进入命中分支，count 加 1，写回，返回。网络请求代码完全不会执行。

代码里没有缓存过期、兜底内容重试、强制刷新机制。命中分支无条件信任本地记录，既不检查 translation 是否为兜底字符串，也不检查记录是否产生于离线状态。因此翻译栏永远显示兜底文案，复习次数每次都 +1。

面板上的"+1 复习"按钮走独立的 handleIncrement，只改 count；保存笔记走 handleUpdateNote，只追加 customEx，都不触碰 translation。popup 列表同样直接读库展示，也不提供删除或重试入口。

### 7. 兜底层级定位

实际存在三层，但只有一层产出用户看到的文案：

- 网络请求层：三个 provider 的 fetch 断网时 reject，不产生文案。
- 策略分发层：三个都失败后 throw，不产生文案。
- 业务编排层：handleLookup 中预先声明的 `let trans = "Translation unavailable"`，catch 住异常后保留该默认值，这就是用户看到的兜底来源。

这句提示不是 fetch 错误信息、不是浏览器离线提示、不是某个 API 返回的错误码文案，也不是前端渲染层兜底，而是 background 业务代码里预先写死的变量初值。

---

## 第二部分：翻译成功但词典接口失败的半条记录

### 1. 场景设定

前两个翻译源超时失败，第三个 MyMemory 成功返回译文，但取音标、词性、例句的词典接口挂掉。

### 2. 面板显示情况

翻译主文本正常显示 MyMemory 返回的中文译文，兜底变量被成功结果覆盖。音标位置整块空白，渲染层对空字符串不输出任何括号或占位符。词性标签不生成。例句区块整体折叠，空数组映射为空字符串，三元判断为假，连区块标题都不出现。笔记区正常显示"暂无笔记"占位项和输入框。复习次数显示 1。

整体是一张"只有单词本身和中文翻译、其余元数据全缺"的卡片，不报错、不提示词典失败、没有重试入口，视觉上无法区分"词本身没有音标例句"和"词典接口当时挂了"。

### 3. 为什么会变成半条记录

翻译请求和词典请求是两个互相独立的 try/catch 块，不是整体事务。词典那一路失败时 catch 块里连日志都没有，直接吞掉异常让元数据保持空对象，主流程照常组装记录、写库、返回成功。代码没有把"翻译成功但词典失败"视为部分失败，也没有给记录打"待补全"标记。

### 4. 后续有没有自动补全机会

没有。第二次划同一个词时本地查库命中，命中分支只做 count 加 1、更新时间戳、写回，然后立即返回。网络请求代码全部不执行，词典接口永远不会再被调用。

命中分支不校验字段完整性，不检查空字段，没有缓存过期或版本校验。手动复习和保存笔记走独立处理器，不碰词典元数据。popup 只读取展示。background 里只有快捷键和消息两类监听器，没有定时任务、启动扫描、网络恢复事件监听，也没有待补全队列。唯一拿到音标的办法是手动删除这条本地记录，让下次划词重新走首次入网路径，但那不是自动补全。

### 5. 与断网兜底场景的共性

两个现象根因相同：handleLookup 在首次入库时把部分成功甚至全失败但有兜底值的结果都当成功记录落库，后续命中分支对缓存内容不加校验地短路返回。脏数据一旦写入就永远不会被自动修正，区别只在于缺失的字段不同。

---

## 第三部分：同一单词不同大小写的归并与拼写显示

### 1. 场景设定

第一次在句首划到大写形式的词（例如 "Apple"），入库；后来在另一篇文章里划到全小写形式（"apple"）。问生词本最后有几条记录，面板上显示的单词拼写以哪一次为准。

### 2. 唯一标识是什么

handleLookup 在开头对传入的 rawWord 做了 `toLowerCase().trim()`，用这个小写化结果作为 word 字段。words 表以 word 为主键，因此主键与用户划到的原始大小写无关。

"Apple" 和 "apple" 小写化后都是 "apple"，指向同一个主键。

### 3. 何时写入、写入什么

第一次划 "Apple" 时本地查库未命中，走到新建记录分支。新记录里：

- word（主键）是小写化的 "apple"。
- displayWord 保留用户原始划词 "Apple"。
- translation、phonetic、pos、systemEx 等来自网络结果。
- count 硬编码为 1。

记录通过 put 写入 IndexedDB。

第二次划 "apple" 时，小写化主键仍是 "apple"，本地查库命中第一条记录，进入命中分支。命中分支不会新建记录，也不会调用 put 写入新对象，它只在已有对象上改 count 和 lastUpdated 后再 put 回去。

因此生词本里最后只有一条记录，主键为 "apple"。

### 4. 面板拼写以哪一次为准

面板标题处渲染逻辑是优先取 data.displayWord，没有时才退回 data.word。命中分支只更新 count 和 lastUpdated，不更新 displayWord，所以 displayWord 一直保持第一次入库时的 "Apple"。

因此无论之后划到的是全小写 "apple"、全大写 "APPLE" 还是其他大小写形式，面板上单词标题始终显示第一次划到的原始拼写 "Apple"。第二次划词只会让复习次数加 1，不会改变显示的拼写。

### 5. 哪些字段写入后还会变

综合三轮，命中分支执行后唯一会变化的字段是 count（每次加 1）和 lastUpdated（更新为当前时间戳）。以下字段一旦在首次入库时写定，后续命中查询都不会变：

- displayWord：保持首次划词的原始大小写。
- translation：保持首次结果，包括首次断网时的兜底文案。
- phonetic、pos、systemEx：保持首次结果，包括首次词典接口失败时的空值。
- customEx：只有用户主动保存笔记时才会追加，命中查询本身不改它。
- word 主键：永远是首次小写化后的值。

这意味着首次入库的网络结果质量决定了这条记录的终身样貌；后续无论网络状况如何改善、划词形式如何变化，都不会触发自动修正。

---

## 第四部分：三轮结论一致性检查

三轮结论不存在矛盾，反而相互印证：

- 第一轮指出命中分支只改 count 和 lastUpdated、不触碰其他字段、不重新发网络请求，这是解释兜底文案固化的依据。
- 第二轮沿用同一依据，得出半条记录的音标例句不会自动补全，与第一轮一致。
- 第三轮继续沿用同一依据，得出 displayWord 不会被后续小写划词更新，面板拼写以第一次为准，同样与第一轮一致。

三轮共同指向同一个根因：首次入库时不区分完整成功、部分成功、全失败兜底，一律落库；后续命中分支对缓存绝对信任且只做计数自增。唯一标识始终是小写化后的 word 主键，写入时机始终是本地未命中时，写入后除 count、lastUpdated 外字段都不再随查询变化。
