# 离线划词兜底与生词本读写链路理解

本文对照本仓库真实实现，解释"断网划词 → 翻译栏显示英文兜底 → 词进生词本 → 联网后仍不刷新、复习次数照涨"这一现象背后的完整代码路径。结论全部落到具体文件与函数行为上，不涉及浏览器扩展离线处理的通识。

---

## 一、一句话结论

那句英文兜底 **不是** 三级翻译策略（Google → Google Web → MyMemory）中的任何一级返回的，而是 [background.js](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/background.js) 里 `handleLookup` 在调用策略之前就预先写死的一个局部变量初值 `"Translation unavailable"`。当三级策略全部抛错时，这个初值被保留，并随着一条 `count: 1` 的新记录被 **无条件写入 IndexedDB**。此后再查同一个词，`handleLookup` 会在本地 DB 命中分支里 **直接短路返回**，既不重新请求网络，也不校验翻译内容是否是兜底值，同时自动把 `count` 加一。所以兜底文案一旦落库就永久粘住，复习次数也随之单调上涨。

---

## 二、模块角色划分

- [manifest.json](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/manifest.json)：声明 V3 架构、`Alt+Q` 快捷键、`storage`/`unlimitedStorage` 权限，以及四个外部接口域名。
- [content.js](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/content.js)：注入页面，负责采集选区、定位浮层、渲染卡片，以及把"+1 复习""保存笔记"等交互再回传给 background。
- [background.js](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/background.js)（Service Worker）：中心化调度。快捷键转发、消息路由、三级翻译策略、词典接口、以及生词本读写全部在这里完成。
- [db.js](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/db.js)：用 Dexie 定义一个名为 `WordVault` 的 IndexedDB 库，只有一张 `words` 表，主键是 `&word`（即小写化后的单词），并对 `count`、`lastUpdated` 建索引。
- [popup.js](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/popup.js)：点插件图标时打开，直接读同一个 Dexie 库，按 `lastUpdated` 倒序列出所有单词。

注意：content script 与 popup 都 **不直接调翻译 API**，网络请求只在 background 里发生。

---

## 三、完整链路（按执行顺序）

### 3.1 快捷键触发

用户按 `Alt+Q` 后，[background.js](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/background.js#L5-L51) 中的 `chrome.commands.onCommand` 监听器被触发，它查询当前活动标签页并向该 tab 发送 `TRIGGER_TRANSLATION` 消息。如果 content script 未就绪（例如刚重载扩展没刷新页面），这里会走 `chrome.scripting.executeScript` 注入一个"请按 F5 刷新"的 toast，这一分支与本次离线问题无关。

### 3.2 选区采集与 loading

[content.js](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/content.js#L7-L11) 收到 `TRIGGER_TRANSLATION` 后调用 [processSelection](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/content.js#L20-L58)：

1. 用 `window.getSelection()` 取选中文本并 `trim`。
2. 空选区弹黑色 toast"获取选区文本失败，请重新选择"；超过 800 字符弹"选区文本过长"。这两种情况直接 return，不会发请求。
3. 通过 `getRangeAt(0).getBoundingClientRect()` 计算浮层坐标。
4. 调用 [showLoading](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/content.js#L62-L74) 在 Shadow DOM 里显示"查询中..."。
5. 向 background 发 `LOOKUP_WORD` 消息，载荷是原始 `rawText`（保留原大小写）。
6. 根据返回的 `res.success` 决定：成功则 `renderPopup`，失败则 `removePopup` 并在控制台打 error。

**关键点**：content 侧只认 `success` 这一个布尔字段，它不关心 `data.translation` 是不是一个有效译文。这是后面"不报错却显示英文"的第一道门。

### 3.3 background 路由到 handleLookup

[background.js](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/background.js#L54-L66) 的消息监听器对 `LOOKUP_WORD` 调用 [handleLookup](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/background.js#L70-L120)，并用 `return true` 保持异步通道开放。

`handleLookup` 首先把入参小写化并 trim 作为主键 `word`，原始文本保留为 `displayWord`。随后逻辑分两个分叉：**A. 本地 DB 命中** 与 **B. 本地未命中走网络**。

### 3.4 分叉 A：本地 DB 命中（复现"联网后仍不刷新"的关键）

[background.js L76-L84](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/background.js#L76-L84)：

- `db.words.get(word)` 命中后，**立刻**执行 `record.count += 1`、更新 `lastUpdated`、`db.words.put(record)`。
- 然后直接 `return { success: true, data: record, source: 'DB' }`。
- 这个分支里 **没有任何 fetch 调用**，也没有判断 `record.translation` 是否等于 `"Translation unavailable"`、是否需要重新拉取。

这就是网络恢复后同词仍显示旧兜底文案的直接原因：记录早已在断网那次落库，之后每次划词都走这个短路分支，永远不会再触发三级策略。同时 `count` 在这里被自动加一，对应"复习次数照样往上涨"。

代码注释里把这个行为写成需求：`// HIT: Auto Increment on lookup (Requirement: "Auto accum")`，即每次查询视为一次复习。

### 3.5 分叉 B：本地未命中，进入三级翻译策略

首次查询断网词时，DB 未命中，进入 [background.js L86-L114](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/background.js#L86-L114)。

执行顺序是：

1. **先给 `trans` 赋初值**：`let trans = "Translation unavailable";`（第 88 行）。
2. 调用 [fetchTranslationStrategy](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/background.js#L152-L169)，它依次尝试：
   - [fetchGoogleStandard](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/background.js#L173-L178)：请求 `translate.googleapis.com`，解析 `json[0][0][0]`。
   - [fetchGoogleWeb](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/background.js#L180-L186)：请求 `translate.google.com`，解析方式相同。
   - [fetchMyMemory](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/background.js#L188-L197)：请求 `api.mymemory.translated.net`，要求 `responseStatus === 200`，否则抛错。
3. 三个 provider 任何一个成功返回字符串，`trans` 就被覆盖；如果都失败，策略函数最终 `throw new Error("All Backups Failed")`。

### 3.6 英文兜底文案的确切来源

关键在于第 89-93 行的 try/catch 是 **局部** 的：

- `fetchTranslationStrategy` 抛出的错误被这一层 catch 吞掉，只在控制台打印 `All translation strategies failed`。
- catch 块里 **没有重新赋值** `trans`，也没有向外抛。
- 因此 `trans` 保留第 88 行的初值 `"Translation unavailable"`。

这是用户看到的那句英文的真正出处。它不属于"三级灾备"中的任何一级——README 里描述的三级灾备在三个 provider 都失败时其实是整体抛错的，真正让流程不崩的是 `handleLookup` 里这层更外层的默认值 + 吞错。换句话说，**四级兜底**：前三失败抛错，第四级是 `handleLookup` 里硬编码的英文字符串。

外层还有一个 try/catch（第 116-119 行），但它只在 DB 操作本身抛异常时才会把 `success` 置为 `false`。翻译失败不会走到这里，所以 content 侧拿到的始终是 `success: true`。

### 3.7 词典元数据的静默降级

翻译尝试之后，第 96-99 行再调用 [fetchDictionaryApi](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/background.js#L199-L222) 取音标、词性、例句。这个请求的失败被一个空 catch 完全忽略，`meta` 保持为空对象 `{}`。于是断网时 `phonetic`、`pos` 为空串，`systemEx` 为空数组——面板上不会显示音标和例句，但也不会报错。这与 README 中"音标例句可能缺失但不影响基础翻译"的描述一致，只是这里连"基础翻译"本身也是兜底字符串。

### 3.8 无条件写入生词本

第 101-113 行组装 `newRecord`：

- `word` 是小写主键，`displayWord` 是原文，`translation` 是 `trans`（此时可能是兜底英文）。
- `count: 1`（首次查询即算一次复习）。
- `customEx: []`，`lastUpdated: Date.now()`。

随后 **不带任何条件判断** 地执行 `await db.words.put(newRecord)`，并返回 `{ success: true, data: newRecord, source: 'API' }`。

这解释了"这个词还进了生词本"：写库动作与翻译是否成功 **完全解耦**，只要入参非空、DB 可写，记录就会被持久化，哪怕译文是占位符。popup 里 [loadWords](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/popup.js#L4-L38) 直接把 `w.translation` 渲染出来，所以生词本里也会看到这句英文。

### 3.9 content 侧渲染面板

[renderPopup](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/content.js#L106-L204) 把 background 返回的 `data` 直接铺进 Shadow DOM 模板：

- 第 150 行 `<div class="meaning">${data.translation}</div>` 直接输出译文，没有做"是否为兜底值"的判断，也没有做 HTML 转义（对本问题无影响，但说明渲染层完全信任 background）。
- 第 164 行显示 `已复习: ${data.count} 次`，这个值来自 DB 记录。
- 第 178-185 行的"+1 复习"按钮向 background 发 `INCREMENT_COUNT`，由 [handleIncrement](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/background.js#L122-L134) 再把 `count` 加一。这是手动累加路径，与查询时自动累加相互独立。
- 第 188-200 行的"保存笔记"走 `UPDATE_NOTE` → [handleUpdateNote](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/background.js#L136-L147)，向 `customEx` 数组 push 一条。

注意一个细节：保存笔记成功后，content 侧第 192 行判断的是 `list.innerText.includes('No notes')`，但模板第 156 行空状态写的是中文"暂无笔记"。这个判断永远为 false，不过它只影响前端清空占位符的小瑕疵，与离线问题无关。

---

## 四、三个现象的代码级归因

### 4.1 为什么"没报错"却显示一句英文

- 三级策略全失败时抛的是 `Error("All Backups Failed")`，但被 [background.js L89-L93](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/background.js#L89-L93) 的局部 catch 吞掉。
- `trans` 保留第 88 行的 `"Translation unavailable"`。
- `handleLookup` 仍返回 `success: true`（只有 DB 异常才会返回 `success: false`）。
- content 侧 [processSelection](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/content.js#L52-L57) 仅凭 `success` 判断，于是正常渲染面板，把这句英文当作译文显示。

所以"不报错"是因为错误被吞在翻译层，没有冒泡到决定 `success` 的那一层。

### 4.2 为什么这个词进了生词本

[background.js L113](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/background.js#L113) 的 `db.words.put(newRecord)` 位于翻译 try/catch 之后、函数返回之前，没有 `if (trans !== "Translation unavailable")` 之类的守卫。只要走到 DB 未命中分支且 DB 可写，记录就一定落库，`count` 初始为 1。

### 4.3 为什么联网后同词仍显示兜底、复习次数照涨

联网后再次划同一个词时，小写化的 `word` 在 Dexie 表里已经存在，于是命中 [3.4 节的分叉 A](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/background.js#L76-L84)：

- 该分支在第 80 行执行 `record.count = (record.count || 0) + 1`，每次查询自动加一，这是"复习次数往上涨"的主因（手动"+1 复习"按钮是另一条叠加路径）。
- 该分支直接返回持久化的 `record`，其中 `translation` 仍是断网时写入的 `"Translation unavailable"`。
- 代码中 **没有** 任何在线状态监听（没有 `chrome.offline`、没有 `navigator.onLine`、没有 `online` 事件）、没有 TTL、没有"译文为兜底值则重试"的判断、也没有提供手动刷新入口。

因此兜底记录一旦写入就成为该词的"权威数据源"，后续所有读取都不会再触网。popup 与面板读到的都是同一条 IndexedDB 记录。

### 4.4 场景延伸：翻译成功但词典接口挂了，面板长什么样、之后还能补全吗

本场景假定前两个翻译源（Google Standard、Google Web）超时抛错，第三个源 MyMemory 成功返回了真实译文，而紧随其后的 [fetchDictionaryApi](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/background.js#L199-L222) 因网络或接口原因抛错。

**首次查询时的执行路径与字段值**

1. [fetchTranslationStrategy](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/background.js#L152-L169) 依次跑完前两个 provider，它们各自抛错并被策略内部的 catch 吞掉、打印日志，随后第三个 [fetchMyMemory](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/background.js#L188-L197) 返回 `responseStatus === 200` 并把 `responseData.translatedText` 赋给 `trans`，覆盖掉第 88 行的初值。
2. 第 96-99 行调用词典接口。它内部 `res.ok` 为 false（或 JSON 解析失败、网络中断）时会 `throw new Error("No Dict Data")`，但这个错误被一个**空 catch** 吞掉，`meta` 保持为 `{}`。
3. 组装 `newRecord` 时：`translation` 是 MyMemory 的真实译文；`phonetic` 为 `meta.phonetic || ""` 即空串；`pos` 为空串；`systemEx` 为 `meta.examples || []` 即空数组；`count: 1`。
4. 第 113 行无条件 `db.words.put(newRecord)`，这条"只有译文、没有音标词性例句"的半残记录被持久化，`source: 'API'`、`success: true`。

**面板实际渲染结果**

content 侧 [renderPopup](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/content.js#L106-L204) 完全按字段是否为空来决定显隐：

- 头部左侧显示 `displayWord`（原大小写选词）；右侧音标位是 `${data.phonetic ? `[${data.phonetic}]` : ''}`，因 `phonetic` 为空串，**方括号连同音标一起不渲染**，只剩单词。
- 词性 tag 受 `${data.pos ? ... : ''}` 控制，`pos` 为空，**不显示任何词性标签**。
- `<div class="meaning">` 正常输出 MyMemory 返回的真实译文。
- 例句区块受 `${sysEx ? ... : ''}` 控制。`sysEx` 由空数组 `map(...).join('')` 得到空字符串，属于 falsy，**整个"例句"分区（含标题与列表）不渲染**。
- "我的笔记"分区始终渲染，初始显示占位符"暂无笔记"，可正常添加。
- 底部"已复习: 1 次"与"+1 复习"按钮正常显示。

也就是说，用户看到的是一张**视觉上"缺了一角"但不报错**的卡片：有单词、有中文译文、有笔记区、有复习计数，但没有音标、没有词性 tag、没有例句区块。这与全断网场景不同——全断网连译文都是英文兜底，而本场景译文是真实的，只是元数据缺失。

**后续是否还有自动补全音标的机会**

没有。判断依据直接沿用第一轮已确立的结论：只要该词已存在于本地 DB，后续任何一次划词都会命中 [background.js L76-L84 的 DB 命中分支](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/background.js#L76-L84)。这个分支只做三件事：读记录、`count += 1`、`put` 回去，然后 `return`。它的函数体内**没有任何 `fetch` 调用**，既不会再走 `fetchTranslationStrategy`，也不会再走 `fetchDictionaryApi`。

具体到本场景：

- 命中分支返回的是持久化对象本身，`phonetic`、`pos`、`systemEx` 维持首次写入时的空值，不会被重新填充。
- 代码中不存在"检测到元数据为空就后台补拉词典"的逻辑，也没有 TTL、版本号、`online` 事件监听或任何手动刷新入口（popup 的 [loadWords](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/popup.js#L4-L38) 只读不写，更不会触发网络请求）。
- 用户唯一能改变记录字段的交互是"保存笔记"（向 `customEx` push）和"+1 复习"（改 `count`），二者都不碰 `phonetic`/`pos`/`systemEx`。

因此，词典接口的这次失败造成的是**永久性数据残缺**：哪怕下一秒网络恢复、`api.dictionaryapi.dev` 完全可用，再划同一个词也只是把 `count` 往上加，音标和例句永远不会回来。要补全只能由用户手动在 IndexedDB 里删记录（或卸载重装扩展清库），让下次查询重新走未命中分支。

这与第一轮英文兜底记录"永久粘住"的机理完全同源——**DB 命中分支无条件信任已有记录、不做任何字段级校验或惰性补全**——只不过这一次粘住的是"空元数据 + 真实译文"，而不是"英文兜底译文"。

### 4.5 场景延伸：句首大写 vs 全小写，生词本有几条、拼写以哪次为准

假定第一次在某文章里划了句首大写形式（例如 "Apple"），第二次在另一篇文章里划了全小写形式（"apple"）。沿用第一轮确立的三个要素来判断：唯一标识是什么、何时写入、写入后哪些字段还会变。

**唯一标识：小写化后的 `word`，不是原始拼写**

- [db.js L4](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/db.js#L4-L4) 的表定义是 `words: '&word, count, lastUpdated'`，`&` 前缀表示 `word` 是主键，主键即唯一标识。
- [background.js L71](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/background.js#L71-L71) 在入库前执行 `const word = rawWord.toLowerCase().trim()`，无论用户划的是 "Apple" 还是 "apple"，主键都归一化为 `"apple"`。
- 原始大小写被另存为 `displayWord`（[L103](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/background.js#L103-L103)），但它不是主键，也没有建索引，不参与去重。

**第一次查询（"Apple"）：写入**

DB 中无 `"apple"`，走未命中分支：新建记录 `word:"apple"`、`displayWord:"Apple"`、`count:1`，第 113 行 `put` 入库。生词本此时有 1 条。

**第二次查询（"apple"）：命中，不新增**

[background.js L76](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/background.js#L76-L76) 用小写化后的 `"apple"` 查到了第一次写入的记录，进入命中分支。该分支在 [L80-L82](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/background.js#L80-L82) 只修改两个字段：`count` 加一、`lastUpdated` 刷新；**完全没有触碰 `displayWord`**，也没有把第二次的入参 `rawWord`（"apple"）回填进去。`put` 是整条覆盖，但覆盖用的是内存里那个 `record` 对象——它的 `displayWord` 仍是第一次的 "Apple"。

跨文章不影响这个结论：content script 虽然按标签页各自注入，但 background Service Worker 和 Dexie/IndexedDB 是整个扩展共享的，`db.words.get("apple")` 在哪篇文章里查都是同一张表、同一条记录。

**最终结果**

- 生词本里只有 **1 条**记录，主键 `"apple"`，`count` 为 2（如果期间还点过"+1 复习"会更高）。
- 面板头部单词显示走 [content.js L146](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/content.js#L146-L146) 的 `${escapeHtml(data.displayWord || data.word)}`；popup 列表走 [popup.js L24](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/popup.js#L24-L24) 同样的兜底逻辑。由于命中分支从不更新 `displayWord`，两处都持续显示第一次入库时的 `"Apple"`，第二次的全小写形式不会生效。
- 换言之，**显示拼写以"第一次成功写入该词时的原始大小写"为准，且之后永远不变**，与之后在任何文章里划到什么大小写无关。只有手动删掉这条记录让下次查询重新走未命中分支，新的拼写才会被写入。

**与前两轮结论是否矛盾**

没有矛盾，反而是同一机制的又一次印证：

- 第一轮说"DB 命中分支只做 count+1 和 lastUpdated，其他字段不动、不触网"——本轮正好验证 `displayWord` 也属于"不动"的字段之一。
- 第二轮 4.4 说"命中分支返回持久化对象本身，字段维持首次写入值，不做字段级补全"——本轮的 `displayWord` 与第二轮的 `phonetic/systemEx` 一样，都是首次写入后被命中分支永久冻结的字段，只是冻结的值不是空串而是 "Apple"。
- 第一轮关于"主键是小写化单词"的描述在本轮直接作为去重判据，与实际代码 [L71](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/background.js#L71-L71) 及 [db.js L4](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/db.js#L4-L4) 完全一致。

唯一需要补充强调的细节是：`displayWord` 是一个"只在首次写入时赋值、之后再无任何代码路径更新"的字段，它和译文、音标、例句一样受命中分支短路保护。这一点前两轮没有单独点出，但与既有结论方向一致，不构成矛盾。

---

## 五、根因归纳

1. **兜底层级错位**：三级翻译策略自身没有"成功兜底"，全失败时抛错；真正兜底的是 `handleLookup` 里一个硬编码英文字符串，且该字符串被当作合法译文参与持久化。
2. **成功判定过宽**：`success` 只反映"DB 是否可写、流程是否跑完"，不反映"是否拿到真实译文"，也不反映"词典元数据是否拿到"，导致 UI 层无法区分完整成功与部分降级。
3. **写库无守卫**：未命中分支无条件 `put`，把占位符译文或缺失元数据的半残记录一律当完整数据永久化；翻译失败、词典失败都不阻止写入。
4. **读路径短路且无字段级补全**：命中分支只累加计数、不刷新，既不校验译文是否为兜底值，也不检测 `phonetic`/`systemEx` 是否为空并惰性补拉词典，任何字段一旦缺失就永久缺失。
5. **缺少在线状态感知**：全仓没有 `online`/`offline` 监听，联网后不会触发任何重试或失效。

这五点叠加，共同造成了"断网写入英文兜底 → 永久粘住 → 计数单调上涨"的可复现行为。

---

## 六、关键代码位置索引

| 关注点 | 位置 |
| --- | --- |
| 快捷键转发与未就绪 toast | [background.js L5-L51](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/background.js#L5-L51) |
| 消息路由 | [background.js L54-L66](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/background.js#L54-L66) |
| handleLookup 主流程 | [background.js L70-L120](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/background.js#L70-L120) |
| DB 命中短路 + 自动计数 | [background.js L76-L84](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/background.js#L76-L84) |
| 英文兜底初值 | [background.js L88](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/background.js#L88-L88) |
| 吞掉三级策略错误 | [background.js L89-L93](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/background.js#L89-L93) |
| 无条件写库 | [background.js L113](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/background.js#L113-L113) |
| 三级策略调度 | [background.js L152-L169](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/background.js#L152-L169) |
| 三个 provider 实现 | [background.js L173-L197](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/background.js#L173-L197) |
| 词典接口（失败静默） | [background.js L199-L222](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/background.js#L199-L222) |
| 手动 +1 复习 | [background.js L122-L134](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/background.js#L122-L134) |
| Dexie 表结构 | [db.js L1-L5](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/db.js#L1-L5) |
| 选区采集与发消息 | [content.js L20-L58](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/content.js#L20-L58) |
| 面板渲染（译文/计数/按钮） | [content.js L106-L204](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/content.js#L106-L204) |
| 生词本列表读取 | [popup.js L4-L38](file:///Users/lilixian/工作相关/AI/ai-eval-workspace/sessions/code-eval-gsb/session-0731/source%20code/gsb0731-709/gsb0731-709-steve/frontend/popup.js#L4-L38) |
